{
  "entities": {
    "LeaderboardEntry": {
      "title": "LeaderboardEntry",
      "description": "A high score entry for the game",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user who achieved the score"
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user"
        },
        "score": {
          "type": "number",
          "description": "The score achieved"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the score was achieved"
        }
      },
      "required": ["userId", "displayName", "score", "timestamp"]
    },
    "FeedbackEntry": {
      "title": "FeedbackEntry",
      "description": "User feedback and suggestions",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the user submitting feedback"
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user"
        },
        "text": {
          "type": "string",
          "description": "The feedback text"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the feedback was submitted"
        }
      },
      "required": ["userId", "displayName", "text", "timestamp"]
    }
  },
  "firestore": {
    "/leaderboard/{entryId}": {
      "schema": {
        "$ref": "#/entities/LeaderboardEntry"
      },
      "description": "Collection of high scores"
    },
    "/feedback/{feedbackId}": {
      "schema": {
        "$ref": "#/entities/FeedbackEntry"
      },
      "description": "Collection of user feedback"
    }
  }
}
